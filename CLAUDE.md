# CLAUDE.md

## Project Overview

- 텍사스 홀덤 포커 게임 프로젝트. 백엔드와 프론트엔드를 별도로 개발한다
- `backend/` — Java 17 + Spring Boot 4.1.1 기반 게임 서버 (Gradle 빌드). 세부 내용은 [backend/CLAUDE.md](backend/CLAUDE.md) 참고
- `frontend/` — 아직 없음. 백엔드 완성 후 웹 프론트엔드를 붙여서 브라우저에서 실제 사람이 플레이할 수 있도록 만들 예정
- MVP 범위: **단일 고정 테이블 1개** (로비/멀티룸/매칭 없음, 최대 인원 고정)

## 게임 규칙 (텍사스 홀덤)

- 플레이어별 홀카드 2장 + 커뮤니티 카드 5장(플랍 3장 → 턴 1장 → 리버 1장)
- 베팅 라운드 순서: PREFLOP → FLOP → TURN → RIVER → SHOWDOWN
- 쇼다운 시 홀카드 2장 + 커뮤니티 카드 5장 중 최적의 5장 조합으로 족보를 판정한다
- 족보 순위(낮음→높음): 하이카드 < 원페어 < 투페어 < 트리플 < 스트레이트 < 플러시 < 풀하우스 < 포카드 < 스트레이트플러시 < 로열플러시
- 액션 종류: CHECK, CALL, BET, RAISE, FOLD, ALL-IN
- 블라인드/바이인/인원수, 레이즈 최소 단위, 사이드팟, 쇼다운 공개 순서 등 상세 스펙은 `backend/docs/requirements.md` 참고

## Scope

- 지금은 **단일 고정 테이블 MVP**에 집중한다. 로비, 멀티룸, 매칭, 로그인/인증은 이후 단계이며 먼저 요청받기 전에는 구현하지 않는다
- 요청하지 않은 리팩터링이나 범위 확장을 임의로 수행하지 않는다

## 협업 방식

- 구현 전에 일방적으로 설계를 확정하지 않는다.
- 새로운 기능을 구현하기 전에는 설계안을 제시하고 사용자와 대화하여 설계를 확정한다.
- 사용자가 "설계해줘", "왜 이렇게 하는지 설명해줘" 등의 요청을 하면 코드를 작성하지 않고 설계와 트레이드오프를 먼저 설명한다.
- 사용자가 명시적으로 구현을 승인하기 전에는 코드를 수정하지 않는다.
- 새로운 설계 결정(예: 동시성 처리 방식, 메시지 프로토콜 형식)이 필요할 때는 미리 상의한 후 진행한다
- 구현 후에는 관련된 테스트만 실행하고 결과를 보고한다
- 작업 완료 후 변경된 파일과 주요 변경 내용을 요약한다

## Git

- 사용자가 요청하지 않는 한 commit이나 push를 수행하지 않는다
